def main():
    string = "araaci"
    k = 2
    print(longest_substring_with_k_distinct(string, k))

def longest_substring_with_k_distinct(str, k):
    # Iterate over the entire array from index 0
    for windowEnd in range(len(str)):
        


main()